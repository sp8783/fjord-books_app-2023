<p style="color: green"><%= notice %></p>

<div class="show-item">
  <%= render @book %>
</div>

<nav class="page-nav">
  <%= link_to t('views.common.link_to_edit'), edit_book_path(@book) %> |
  <%= link_to t('views.common.link_back_to_index', item: Book.model_name.human), books_path %>
  <%= button_to t('views.common.link_to_destroy'), @book, method: :delete %>
</nav>
